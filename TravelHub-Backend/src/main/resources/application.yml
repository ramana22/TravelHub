spring:
  server:
    port: 8080
    address: 0.0.0.0
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql-standalone:3386/travelhub?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root
    hikari:
      initialization-fail-timeout: 0
      maximum-pool-size: 10  # Example: Adjust according to your application's requirements
      minimum-idle: 5  # Example: Adjust according to your application's requirements
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
